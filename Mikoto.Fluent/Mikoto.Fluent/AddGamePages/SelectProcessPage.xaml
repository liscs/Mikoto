<?xml version="1.0" encoding="utf-8"?>
<local:BaseStepPage
    x:Class="Mikoto.Fluent.AddGamePages.SelectProcessPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Mikoto.Fluent.AddGamePages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:local1="using:Mikoto.Core.Models"
                    xmlns:local11="using:Mikoto.Core.Models.AddGame"
                    mc:Ignorable="d">

    <Grid RowDefinitions="Auto, *">
        <StackPanel Grid.Row="0" Spacing="4" Canvas.ZIndex="1">
            <InfoBar Title="权限提示"
                 Message="建议以管理员身份运行以获取完整进程列表。"
                 Severity="Warning"
                 IsOpen="{x:Bind ViewModel.IsAdminWarningVisible, Mode=OneWay}"
                 IsClosable="True" />

            <InfoBar x:Name="SelectionErrorBar"
                 Title="未选择进程"
                 Message="请先从列表中选择一个要添加的游戏进程。"
                 Severity="Error"
                 IsOpen="{x:Bind ViewModel.ShowError, Mode=TwoWay}" />
        </StackPanel>

        <ListView Grid.Row="1"
              ItemsSource="{x:Bind ViewModel.Processes, Mode=OneWay}"
              SelectedItem="{x:Bind ViewModel.SelectedProcess, Mode=TwoWay}"
              SelectionMode="Single"
              Padding="12"
              Margin="0,8,0,0">

            <ListView.Header>
                <TextBlock Text="可用进程列表" 
                       Style="{StaticResource BodyStrongTextBlockStyle}" 
                       Margin="4,0,0,12" />
            </ListView.Header>

            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local11:ProcessItem">
                    <Grid Padding="4,4">
                        <StackPanel Spacing="2">
                            <TextBlock Text="{x:Bind Title}"
                   Style="{StaticResource BodyTextBlockStyle}"
                   FontWeight="SemiBold"
                   TextTrimming="CharacterEllipsis" />

                            <StackPanel Orientation="Horizontal" Spacing="5">
                                <TextBlock Text="{x:Bind Name}"
                       FontSize="12"
                       Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" />

                                <TextBlock Text="{x:Bind Id}"
                       FontSize="12"
                       Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" />
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</local:BaseStepPage>

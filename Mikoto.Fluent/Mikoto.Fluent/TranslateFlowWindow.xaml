<?xml version="1.0" encoding="utf-8"?>
<winuiex:WindowEx
    xmlns:winuiex="using:WinUIEx"
    x:Class="Mikoto.Fluent.TranslateFlowWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Mikoto.Fluent"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:local1="using:Mikoto.Core.Models"
    mc:Ignorable="d"
    Title="TranslateFlowWindow">

    <Window.SystemBackdrop>
        <winuiex:TransparentTintBackdrop TintColor="#AA000000" />
    </Window.SystemBackdrop>

    <Grid x:Name="RootGrid" 
          Background="Transparent" 
          PointerPressed="Root_PointerPressed" 
          PointerMoved="Root_PointerMoved" 
          PointerReleased="Root_PointerReleased">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="1" 
                 Text="{x:Bind ViewModel.OriginalText, Mode=OneWay}" 
                 Margin="12,10,12,8" Foreground="White"/>

        <ScrollViewer Grid.Row="2" 
                      VerticalScrollBarVisibility="Auto" 
                      Padding="12,0,12,12">
            <ItemsControl ItemsSource="{x:Bind ViewModel.MultiTranslateResults, Mode=OneWay}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="local1:TranslationResult">
                        <Grid Margin="0,0,0,8">
                            <StackPanel>
                                <Grid>
                                    <TextBlock Text="{x:Bind ResultText, Mode=OneWay}"
                                             TextWrapping="Wrap"
                                             FontSize="24" Foreground="White"/>
                                </Grid>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</winuiex:WindowEx>